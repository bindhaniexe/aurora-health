<div align="center">

<!-- Replace with your actual banner/logo image -->
![Aurora Banner](./assets/images/icon.png)

# ✨ Aurora

### Your AI-Powered Personal Health Companion

[![Expo](https://img.shields.io/badge/Expo-SDK%2054-000020?style=for-the-badge&logo=expo&logoColor=white)](https://expo.dev)
[![React Native](https://img.shields.io/badge/React%20Native-0.81-61DAFB?style=for-the-badge&logo=react&logoColor=white)](https://reactnative.dev)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9-3178C6?style=for-the-badge&logo=typescript&logoColor=white)](https://typescriptlang.org)
[![Supabase](https://img.shields.io/badge/Supabase-Postgres-3ECF8E?style=for-the-badge&logo=supabase&logoColor=white)](https://supabase.com)

*Hydration · Sleep · Habits · AI Voice Companion · Daily Insights*

</div>

---

## 📸 Screenshots

> **Tip:** Drop your screenshots here once you have them!

| Dashboard | Hydration | Sleep | Habits |
|:---------:|:---------:|:-----:|:------:|
| ![Dashboard](./docs/screenshots/dashboard.png) | ![Hydration](./docs/screenshots/hydration.png) | ![Sleep](./docs/screenshots/sleep.png) | ![Habits](./docs/screenshots/habits.png) |

| AI Companion | Profile | Onboarding | Auth |
|:------------:|:-------:|:----------:|:----:|
| ![Companion](./docs/screenshots/companion.png) | ![Profile](./docs/screenshots/profile.png) | ![Onboarding](./docs/screenshots/onboarding.png) | ![Auth](./docs/screenshots/auth.png) |

---

## 🌟 What is Aurora?

Aurora is a **mobile-first AI health companion** that makes health tracking feel natural and motivating. Instead of manually logging data into forms, you simply **talk to Aurora** — it understands your context, logs the data for you, and gives you personalized daily insights.

### Core Experience

- 🎙️ **Voice-first interaction** — tell Aurora "I drank 500ml of water" and it logs it
- 💧 **Hydration tracking** with an animated virtual water bottle
- 😴 **Sleep tracking** and weekly trend analysis
- ✅ **Habit creation, streaks, and completion** tracking
- 🧠 **Daily AI insight cards** generated by Groq for personalized motivation
- 🎨 **Beautiful, vibrant UI** with purple-pink gradient accents and smooth animations

---

## 🏗️ Architecture Overview

```
Aurora/
├── app/                    # Expo Router — screens only
│   ├── (auth)/             # Login & Sign Up
│   ├── (onboarding)/       # Welcome + health goal setup
│   ├── (tabs)/             # Main tab navigation
│   │   ├── index.tsx       # Dashboard
│   │   ├── hydration.tsx   # Water tracking + bottle animation
│   │   ├── sleep.tsx       # Sleep logging + weekly chart
│   │   ├── habits.tsx      # Habit management + streaks
│   │   ├── companion.tsx   # Voice AI companion screen
│   │   └── profile.tsx     # User profile + settings
│   └── summary.tsx         # Daily health summary
│
├── components/             # Reusable UI components
│   ├── WaterBottle.tsx     # Animated SVG water bottle
│   ├── HydrationCard.tsx   # Dashboard hydration widget
│   ├── SleepCard.tsx       # Sleep stat card (gradient surface)
│   ├── HabitsCard.tsx      # Habit overview card
│   ├── HabitItem.tsx       # Single habit row
│   ├── InsightBanner.tsx   # AI-generated insight strip
│   ├── MicButton.tsx       # Voice companion mic (idle/listening/speaking)
│   ├── StepsCard.tsx       # Steps & activity card
│   ├── StreakCard.tsx       # Streak display card
│   ├── CalorieGoalCard.tsx # Calorie/nutrition goal card
│   └── MealsToday.tsx      # Meals log component
│
├── services/               # All Supabase data access
│   ├── hydrationService.ts
│   ├── sleepService.ts
│   ├── habitService.ts
│   ├── insightService.ts   # Groq insight cache + generation
│   ├── agentService.ts     # Voice agent function execution
│   └── profileService.ts
│
├── stores/                 # Zustand global state
│   ├── authStore.ts
│   ├── hydrationStore.ts
│   ├── sleepStore.ts
│   ├── habitStore.ts
│   ├── healthStore.ts
│   ├── nutritionStore.ts
│   ├── companionStore.ts
│   └── profileStore.ts
│
├── lib/                    # External service clients
│   ├── supabase.ts         # Supabase client
│   ├── groq.ts             # Groq AI client
│   ├── realtimeAgent.ts    # OpenAI Realtime API connection
│   └── cn.ts               # NativeWind class merge utility
│
├── hooks/                  # Custom hooks
├── constants/              # Design tokens (colors, gradients, spacing)
├── types/                  # Shared TypeScript interfaces
├── utils/                  # Pure utility functions
└── supabase/
    ├── migrations/         # PostgreSQL schema migrations
    └── functions/          # Supabase Edge Functions
        └── realtime-session/  # Ephemeral OpenAI token endpoint
```

---

## 🤖 AI Features

### Voice Companion (OpenAI Realtime API)

The voice companion is the heart of Aurora. Tap the mic and speak naturally:

> *"I drank 2 glasses of water"* → logs 500ml  
> *"I slept for 7.5 hours last night"* → logs sleep  
> *"Add a new habit: morning stretch"* → creates the habit  
> *"How am I doing today?"* → AI summarizes your progress

**Agent Tools the AI can call:**

| Tool | Description |
|------|-------------|
| `log_water(amount_ml)` | Logs a hydration entry |
| `log_sleep(hours, quality?)` | Logs a sleep session |
| `create_habit(name, frequency)` | Creates a new habit |
| `complete_habit(habit_name)` | Marks a habit complete for today |
| `get_health_summary()` | Fetches today's data for context |

**Security:** The OpenAI API key never lives in the client. A Supabase Edge Function (`realtime-session`) creates an ephemeral session token and returns it to the app.

### Daily Insights (Groq — llama-3.3-70b)

Each morning, Aurora generates a personalized, single-sentence motivational insight based on your health data. Results are cached daily in Supabase so the API is only called once per day.

---

## 🛠️ Tech Stack

| Layer | Technology |
|-------|-----------|
| **Framework** | Expo SDK 54 + Expo Router (file-based routing) |
| **Language** | TypeScript 5.9 |
| **UI** | React Native 0.81.5 + NativeWind v5 (Tailwind CSS) |
| **Gradients** | `expo-linear-gradient` |
| **Animations** | React Native Reanimated v4 |
| **SVG** | `react-native-svg` (water bottle, progress rings) |
| **State** | Zustand v5 |
| **Backend** | Supabase (Auth, PostgreSQL, Storage, Edge Functions) |
| **Voice AI** | OpenAI Realtime API (`gpt-4o-realtime-preview`) |
| **Text AI** | Groq (`llama-3.3-70b-versatile`) |
| **Audio** | Expo AV (capture + playback) |
| **Secure Storage** | Expo Secure Store |
| **Icons** | Lucide React Native + Expo Vector Icons |

---

## 🗄️ Database Schema

Managed via Supabase migrations:

| Table | Purpose |
|-------|---------|
| `profiles` | User profile, goals, preferences, memory notes |
| `hydration_logs` | Per-drink water intake entries |
| `sleep_logs` | Nightly sleep sessions with quality rating |
| `habits` | User-defined habits with frequency config |
| `habit_completions` | Daily completion records (streak logic) |
| `insights_cache` | Cached daily Groq insight cards |

All tables have **Row Level Security** — users can only access their own data.

---

## 🎨 Design System

Aurora uses a curated design system with tokens defined in `constants/`:

**Color Palette:**
- Background: `#FFFFFF` (cards), `#F7F5FF` (auth — soft lavender)
- Brand gradient: `#6D28D9` → `#A855F7` → `#EC4899` (deep violet → purple-pink)
- Accents: `#7C3AED` (purple), `#EC4899` (pink), `#10B981` (green/success)
- Text: `#1E1B2E` (primary), `#6B7280` (secondary)

**Typography:** Poppins Bold (headings/numbers) · Inter (body text)

**Design Mood:** Energetic · Clean · Motivating · Modern · Vibrant

---

## 🚀 Getting Started

### Prerequisites

- Node.js 18+
- [Expo CLI](https://docs.expo.dev/get-started/installation/)
- [Expo Go](https://expo.dev/go) app on your phone, **or** an Android/iOS emulator
- A Supabase project (free tier works)
- OpenAI API key (for voice companion)
- Groq API key (for daily insights)

### 1. Clone & Install

```bash
git clone https://github.com/YOUR_USERNAME/aurora.git
cd aurora
npm install
```

### 2. Configure Environment

Create a `.env.local` file in the project root:

```env
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
EXPO_PUBLIC_GROQ_API_KEY=your-groq-api-key
```

> ⚠️ **Never commit your `.env.local` file.** It is already in `.gitignore`.

### 3. Set Up the Database

Push the migrations to your Supabase project:

```bash
npx supabase db push
```

Or run them manually in the Supabase SQL Editor from `supabase/migrations/`.

### 4. Deploy the Edge Function

```bash
npx supabase functions deploy realtime-session
```

Set your OpenAI key as a Supabase secret:

```bash
npx supabase secrets set OPENAI_API_KEY=your-openai-api-key
```

### 5. Start the App

```bash
npm start          # Expo Go (scan QR code)
npm run android    # Android emulator / device
npm run ios        # iOS simulator / device
```

---

## 📦 Building for Production

### Android APK (Preview)

```bash
eas build --profile preview --platform android
```

### Android / iOS (Production)

```bash
eas build --profile production --platform android
eas build --profile production --platform ios
```

---

## 📁 Project Codegraph

> **Add your Codegraph visualization here.**

<!-- 
  Replace this placeholder with your actual Codegraph image:
  
  ![Aurora Codegraph](./docs/codegraph.png)
  
  Or embed an interactive Codegraph URL if available.
-->

```
[ 📊 Codegraph visualization placeholder ]
[ Drop your codegraph.png into docs/ and uncomment the line above ]
```

---

## 🤝 Contributing

This project was built as a hackathon MVP. Contributions, bug reports, and feature suggestions are welcome!

1. Fork the repo
2. Create a feature branch: `git checkout -b feature/my-feature`
3. Commit your changes: `git commit -m 'feat: add my feature'`
4. Push and open a PR

---

## 📄 License

MIT License — see [LICENSE](./LICENSE) for details.

---

<div align="center">

Built with 💜 using Expo, Supabase, and OpenAI

</div>
