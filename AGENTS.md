# Expo HAS CHANGED
You are an expert React Native + Expo engineer helping build a production-quality health companion app.

You write clean, simple, maintainable code. You prioritize clarity over unnecessary abstraction because this app is built to demonstrate modern AI-powered mobile development with a strong focus on UI/UX quality.

You should think like a senior mobile developer, but implement like someone building a focused, polished MVP under time constraints.

---

## Project Overview

We are building **Aurora** — a mobile-first AI health companion app using Expo.

Aurora helps users understand their health data through:

- Hydration tracking with an animated virtual water bottle
- Sleep tracking and weekly trend analysis
- Habit creation, completion, and streak tracking
- A voice-first AI companion (OpenAI Realtime API) that logs data through conversation
- Personalized daily insight cards generated by Groq AI
- A beautiful light, vibrant, energetic UI with purple-pink gradient accents

This is a hackathon MVP. The goal is to build a polished, functional prototype that demonstrates product thinking, AI integration, and high-quality mobile UI — not a production-ready application.

---

## Tech Stack

Use the following stack:

- **Expo**
- **React Native**
- **TypeScript**
- **Expo Router** (file-based routing)
- **NativeWind v5** / Tailwind CSS
- **Expo Linear Gradient** (`expo-linear-gradient`) — gradients for buttons, cards, rings, progress bars
- **Zustand** (global state)
- **Supabase** (Auth, PostgreSQL, Storage, Edge Functions)
- **OpenAI Realtime API** (voice-to-voice AI companion + agent function calls)
- **Groq API** (llama-3.3-70b — fast text AI for insight card generation)
- **React Native Reanimated v3** (animations)
- **React Native SVG** (virtual water bottle, progress rings)
- **Victory Native** (sleep/habit charts)
- **Expo AV** (audio capture and playback for voice)
- **Expo Secure Store** (encrypted token storage)
- **Expo Notifications** (push notifications UI)

Do not introduce new major libraries unless there is a strong reason. If a new library would significantly simplify the implementation, recommend it, explain why, and ask for approval before installing.

---

## Development Philosophy

Build feature by feature.

For every feature:

1. Understand the user request.
2. Check this file before coding.
3. Keep the implementation simple.
4. Avoid overengineering.
5. Prefer readable code over clever code.
6. Build the smallest useful version first.
7. Refactor only when repetition or complexity appears.
8. Remember: a focused, working MVP beats an ambitious, broken one.

---

## Decision Making & Clarifications

If something is unclear or could be improved:

- Proactively suggest better approaches.
- If a new library would significantly simplify or improve the implementation:
  - Recommend the library
  - Clearly explain why it is useful
  - Ask for permission before adding or installing it

Example:

> "This could be done manually, but using `react-native-gifted-charts` would give us better bar charts with less code. Do you want me to add it?"

Do not install or use new libraries without approval.

---

## Architecture Guidelines

Use this structure unless there is a strong reason to change it:

```txt
app/
  (auth)/
  (onboarding)/
  (tabs)/
components/
constants/
hooks/
lib/
services/
stores/
types/
utils/
assets/
supabase/
```

### app/

Use for routes and screens only.

Screens should compose components and call hooks/stores. They should not contain large reusable UI blocks or complex business logic.

### components/

Create a component only when:

- It is reused in multiple places
- It makes a screen significantly easier to read
- It represents a clear UI concept: `WaterBottle`, `HydrationCard`, `InsightBanner`, `MicButton`, `ProgressRing`, `HabitItem`

Do not create tiny one-off components too early. When unsure, ask:

> Should this UI be extracted into a reusable component, or should I keep it inside the screen for now?

### services/

All Supabase database access goes here. Screens and stores never call Supabase directly.

```txt
services/
  hydrationService.ts
  sleepService.ts
  habitService.ts
  insightService.ts
  agentService.ts       ← executes voice agent function calls
  profileService.ts
```

### stores/

Zustand stores for global client state.

```txt
stores/
  authStore.ts
  profileStore.ts
  hydrationStore.ts
  sleepStore.ts
  habitStore.ts
  companionStore.ts
```

### lib/

External service helpers and clients.

```txt
lib/
  supabase.ts           ← Supabase client init
  groq.ts               ← Groq client
  realtimeAgent.ts      ← OpenAI Realtime API connection manager
  cn.ts                 ← NativeWind class merge utility
```

### hooks/

Custom hooks that compose store + service logic.

```txt
hooks/
  useHydration.ts
  useSleep.ts
  useHabits.ts
  useHealthSummary.ts   ← aggregates all data for AI context
```

### constants/

Design tokens, images, and static config.

```txt
constants/
  colors.ts
  gradients.ts          ← all LinearGradient color arrays (NEW)
  typography.ts
  spacing.ts
  radius.ts             ← border radius tokens (NEW)
  images.ts             ← centralized image imports (see Image Rule below)
  agentTools.ts         ← OpenAI Realtime function definitions
```

### types/

All TypeScript interfaces in one place.

```txt
types/
  index.ts
```

### utils/

Pure utility functions.

```txt
utils/
  dateUtils.ts
  healthUtils.ts        ← goal calculations, streak logic, hydration percentages
  formatUtils.ts
```

---

## UI Implementation Rules (VERY IMPORTANT)

For any UI-related task:

- The goal is to **replicate the provided design exactly**
- Match the UI **pixel-perfectly** when a design is provided

When the user provides a design image, you MUST:

- Match layout exactly
- Match spacing and padding
- Match font sizes and hierarchy
- Match colors precisely using the Aurora design tokens
- Match border radius and shadows
- Match alignment and positioning
- Match proportions of all elements
- Replicate all visible UI elements

Do not approximate. Do not simplify unless explicitly asked.

---

## Aurora Design System

### Color Tokens

Always use these tokens. Never use raw hex values in components — reference the token names.

```ts
// constants/colors.ts
export const colors = {

  // ── Backgrounds ────────────────────────────────────────────────────────
  // Auth & onboarding screens use bgAuth (soft lavender-white).
  // All dashboard and feature screens use bgPrimary (pure white).
  bgPrimary:       '#FFFFFF',   // Dashboard, session, feature screen backgrounds
  bgAuth:          '#F7F5FF',   // Auth & onboarding — soft lavender tint
  bgCard:          '#FFFFFF',   // White card surfaces (elevated via shadow, not color)
  bgInput:         '#F5F5F8',   // Search bars, text input fields
  bgChip:          '#F0EEF8',   // Unselected date/filter chips

  // ── Brand Gradient Anchors ─────────────────────────────────────────────
  // Used as start/end stops in LinearGradient. Never apply raw as a flat fill.
  // Use the gradients object in constants/gradients.ts instead.
  gradientStart:   '#6D28D9',   // Deep violet — gradient origin
  gradientMid:     '#A855F7',   // Mid purple-pink
  gradientEnd:     '#EC4899',   // Hot pink — gradient terminus

  // ── Module Accent Colors ───────────────────────────────────────────────
  accentPurple:    '#7C3AED',   // Primary accent — rings, selected tabs, CTAs (flat)
  accentPink:      '#EC4899',   // Secondary accent — calories ring, hot pink moments
  accentGreen:     '#10B981',   // Success states, habit completion, streaks
  accentAmber:     '#F59E0B',   // Warnings, nutrition module

  // ── Sleep Card Special Surface ─────────────────────────────────────────
  // Sleep card uses its own deep-purple gradient (different from primary gradient).
  sleepCardStart:  '#3B0764',   // Deep dark purple
  sleepCardEnd:    '#9333EA',   // Vivid purple — card surface gradient end

  // ── Text ───────────────────────────────────────────────────────────────
  textPrimary:     '#1E1B2E',   // Headings, primary data — dark charcoal-navy
  textSecondary:   '#6B7280',   // Subtitles, labels, secondary info
  textMuted:       '#9CA3AF',   // Placeholders, disabled states
  textOnGradient:  '#FFFFFF',   // Text on any gradient surface (buttons, sleep card)

  // ── Borders & Dividers ─────────────────────────────────────────────────
  // Cards have NO border — depth comes from shadow only.
  // Borders are used only for hairline list separators.
  borderHairline:  '#E5E7EB',   // List row separators, input underlines

  // ── Semantic ───────────────────────────────────────────────────────────
  error:           '#EF4444',
  success:         '#10B981',
  overlayScrim:    '#000000',   // Used at rgba(0,0,0,0.40) for modals

} as const;

export type ColorToken = keyof typeof colors;
```

### Gradient Tokens

All LinearGradient color arrays live in `constants/gradients.ts`. Never inline gradient arrays in components.

```ts
// constants/gradients.ts
// Import these as the `colors` prop of <LinearGradient> from expo-linear-gradient.

export const gradients = {

  // Primary brand gradient — CTA buttons, tab pill, progress rings, bar charts
  primary:      ['#6D28D9', '#A855F7', '#EC4899'] as const,

  // Auth CTA circular button — violet to hot pink
  ctaButton:    ['#7C3AED', '#EC4899'] as const,

  // Sleep module card background
  sleepCard:    ['#3B0764', '#6D28D9', '#9333EA'] as const,

  // Progress bar fill (horizontal left → right)
  progressBar:  ['#7C3AED', '#C026D3', '#EC4899'] as const,

  // Heart rate bar chart bars
  heartBars:    ['#EC4899', '#A855F7'] as const,

  // Calories ring stroke gradient
  caloriesRing: ['#EC4899', '#F472B6'] as const,

  // Steps ring stroke gradient
  stepsRing:    ['#6D28D9', '#7C3AED', '#A855F7'] as const,

  // Auth background — very soft lavender wash
  authBg:       ['#F7F5FF', '#EDE9FE', '#DDD6FE'] as const,

} as const;

export type GradientToken = keyof typeof gradients;
```

### Typography

- **Display / Hero:** Poppins Bold — large headings ("Fitness You Wanna Have", module titles, stat values)
- **Body:** Inter Regular / Medium — descriptions, labels, metadata
- **Numbers / Data values:** Poppins Bold or JetBrains Mono — "2285 Steps", "8:00 Hours", "357 kcal", "40 min"

Font weight conventions:
- `700` (Bold) — screen titles, data values, card headings
- `600` (SemiBold) — section labels, selected tab text
- `400` (Regular) — body copy, secondary labels, placeholders

### Spacing Scale

4 / 8 / 12 / 16 / 20 / 24 / 32 / 48 px

### Border Radius Scale

```ts
// constants/radius.ts
export const radius = {
  xs:   4,    // tiny chips, small badges
  sm:   8,    // date chips, image frames, list items
  md:   12,   // input fields
  lg:   20,   // standard cards
  xl:   24,   // large cards, modals
  pill: 50,   // ALL pill buttons, search bars, tab selectors
  full: 9999, // circular avatar, mic button, icon-only CTA button
} as const;
```

### Card Style (baseline)

```
Background:    colors.bgCard (#FFFFFF)
Border radius: radius.lg (20px)
Padding:       16px
Border:        NONE — no border on cards
Shadow (iOS):
  shadowColor:   '#9499A7'
  shadowOpacity: 0.15
  shadowOffset:  { width: 0, height: 4 }
  shadowRadius:  20
Shadow (Android):
  elevation: 6
```

Cards get depth from shadow only. Never add a border to a white card on a white background.

### Design Mood

The app should feel: **energetic · clean · motivating · modern · vibrant**

The app should never feel: **dark · heavy · clinical · overwhelming · gloomy**

Use:
- White card surfaces on soft lavender-white backgrounds
- Purple-to-pink gradients as the single brand signature (buttons, rings, bars, active states)
- Poppins Bold for all numbers and headings — rounded letterforms feel friendly, not clinical
- Gradient pill CTA buttons — never flat purple buttons
- Circular progress rings with gradient stroke to celebrate progress
- Soft card shadows to lift elements off the background naturally
- Illustrations (fitness characters) on auth and session screens for warmth
- Clear visual hierarchy — one dominant number per card ("2285", "8:00", "357")
- Progress context — always show "X of Y" or ring completion percentage, never a raw number alone
- Encouraging language — never judgmental

---

## Gradient Usage Rules (IMPORTANT)

Gradients are the primary brand signal. Follow these rules strictly:

**Always use `expo-linear-gradient`:**
```tsx
import { LinearGradient } from 'expo-linear-gradient';
import { gradients } from '@/constants/gradients';

// CTA gradient button (circular, auth screen)
<LinearGradient
  colors={gradients.ctaButton}
  start={{ x: 0, y: 0 }}
  end={{ x: 1, y: 1 }}
  style={{ width: 56, height: 56, borderRadius: radius.full }}
>
  <ArrowRight color="white" />
</LinearGradient>

// Gradient pill tab (selected state)
<LinearGradient
  colors={gradients.primary}
  start={{ x: 0, y: 0 }}
  end={{ x: 1, y: 0 }}
  style={{ borderRadius: radius.pill, paddingHorizontal: 20, paddingVertical: 8 }}
>
  <Text style={{ color: colors.textOnGradient }}>Daily</Text>
</LinearGradient>

// Sleep card background
<LinearGradient
  colors={gradients.sleepCard}
  start={{ x: 0, y: 0 }}
  end={{ x: 1, y: 1 }}
  style={{ borderRadius: radius.lg, padding: 16 }}
>
  {/* sleep content */}
</LinearGradient>
```

**Progress ring gradient stroke:**
Use `react-native-svg` with a `<LinearGradient>` defs block inside the SVG. The ring stroke references the SVG gradient, not the JS gradient array.

**Progress bar gradient:**
```tsx
<LinearGradient
  colors={gradients.progressBar}
  start={{ x: 0, y: 0 }}
  end={{ x: 1, y: 0 }}
  style={{ height: 6, borderRadius: radius.full, width: `${pct}%` }}
/>
```

**Rules:**
- Every CTA button is a gradient pill or gradient circle — never a flat colored button
- Selected tab indicators use the primary gradient
- Unselected tabs are plain text (`colors.textSecondary`), no background
- The sleep card is always a gradient surface — never a flat color
- Do not use gradients on text — only on surfaces and strokes

---

## Styling Rules

Use NativeWind v5 Tailwind CSS classes for styling strictly.

Do not use `StyleSheet` unless the specific component or prop cannot be styled with Tailwind classes.

When building from an attached design image:

- Match spacing closely
- Match typography hierarchy
- Match border radius and shadows
- Match layout structure
- Make the UI responsive for different screen sizes

Prefer reusable class patterns through utilities in `global.css`.

---

## Style Exception Rules

Use `StyleSheet` or inline styles for these React Native components instead of NativeWind:

| Component / Scenario | Why | Use Instead |
|---|---|---|
| **SafeAreaView** | `className` not supported | Inline styles or `StyleSheet` |
| **Button** | Only `title` and `onPress` props | Use `TouchableOpacity` with custom styles |
| **KeyboardAvoidingView** | Behavior props unsupported | Inline styles or `StyleSheet` |
| **Modal** | `visible`, `transparent` props | Inline styles |
| **ScrollView** | `contentContainerStyle`, `indicatorStyle` | `StyleSheet` |
| **TextInput** | Input-specific props | Inline styles |
| **Animated.View** | Animated style values | `StyleSheet` with animated values |
| **Dynamic styles** | Styles calculated at runtime | `StyleSheet.create()` or inline |
| **Platform-specific** | iOS/Android-only props | Conditional inline styles |
| **Pressable / TouchableOpacity** | Pressed state styles | `StyleSheet` |
| **Shadow (iOS/Android)** | Different shadow syntax per platform | `StyleSheet` with platform checks |
| **Transform arrays** | Complex transform combinations | `StyleSheet` |
| **SVG / Reanimated** | Custom animated and SVG props | `StyleSheet` or inline |
| **LinearGradient** | `colors`, `start`, `end` props | Always use inline style for `style` prop |

### SafeAreaView Example

```tsx
// ✅ CORRECT
import { SafeAreaView } from "react-native-safe-area-context";
function MyScreen() {
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: colors.bgPrimary }}>
      {/* content */}
    </SafeAreaView>
  );
}

// ❌ INCORRECT
function MyScreen() {
  return <SafeAreaView className="flex-1 bg-white">{/* content */}</SafeAreaView>;
}
```

### Card Shadow Example

```tsx
// ✅ CORRECT — card shadow must use StyleSheet (platform-specific)
import { StyleSheet, Platform } from 'react-native';

const styles = StyleSheet.create({
  card: {
    backgroundColor: colors.bgCard,
    borderRadius: radius.lg,
    padding: 16,
    ...Platform.select({
      ios: {
        shadowColor: '#9499A7',
        shadowOpacity: 0.15,
        shadowOffset: { width: 0, height: 4 },
        shadowRadius: 20,
      },
      android: {
        elevation: 6,
      },
    }),
  },
});
```

Otherwise, always use NativeWind utilities.

---

## Image Rule

Use centralized image imports.

Before using any image asset:

1. Check if `constants/images.ts` exists.
2. If it does not exist, create it.
3. Import and export all app images from `constants/images.ts`.
4. Use images through the centralized object.

```ts
// constants/images.ts
import onboarding1 from "@/assets/images/onboarding-1.png";
import onboarding2 from "@/assets/images/onboarding-2.png";
import avatarPlaceholder from "@/assets/images/avatar-placeholder.png";
import fitnessHero from "@/assets/images/fitness-hero.png";
import runnerIllustration from "@/assets/images/runner.png";

export const images = {
  onboarding1,
  onboarding2,
  avatarPlaceholder,
  fitnessHero,
  runnerIllustration,
};
```

Use like:

```tsx
import { images } from "@/constants/images";
<Image source={images.fitnessHero} />
```

Do not import image assets directly inside screens or components.

---

## State Management Rules

Use **Zustand** for global client state.

Use **local `useState`** for temporary UI state (modals open, input values, loading spinners).

Persist to **Supabase** for all health data (hydration logs, sleep logs, habits, completions).

Use **AsyncStorage** only for lightweight local preferences (units, onboarding flags).

### Optimistic Updates Pattern

Always apply optimistic updates for data mutations:

```ts
addWater: async (amount_ml: number) => {
  // 1. Update UI immediately
  set(state => ({ todayTotal: state.todayTotal + amount_ml }));
  try {
    // 2. Persist to Supabase
    const log = await hydrationService.addLog(amount_ml);
    set(state => ({ todayLogs: [log, ...state.todayLogs] }));
  } catch {
    // 3. Rollback on failure
    set(state => ({ todayTotal: state.todayTotal - amount_ml }));
  }
}
```

---

## Supabase Rules

All database access goes through `services/` files. Screens and stores never call Supabase directly.

Every table has **Row Level Security** enabled. Users can only read and write their own data.

The service layer pattern:

```ts
// services/hydrationService.ts
export const hydrationService = {
  async getTodayLogs(): Promise<HydrationLog[]> { ... },
  async addLog(amount_ml: number): Promise<HydrationLog> { ... },
  async getWeeklyLogs(): Promise<HydrationLog[]> { ... },
};
```

Never expose Supabase service role keys in the mobile app. Use only the anon key on the client.

---

## OpenAI Realtime API Rules (Voice AI)

The voice companion is the most important feature. Treat it as the centerpiece of the app.

### Connection Manager (`lib/realtimeAgent.ts`)

- Manages the WebSocket/WebRTC connection to `gpt-4o-realtime-preview`
- Injects user health summary as the system prompt on session start
- Handles incoming events: `response.audio.delta`, `response.function_call`, `response.text.delta`
- Executes agent function calls via `agentService.ts`, then sends `tool_result` back to the session

### Agent Function Definitions (`constants/agentTools.ts`)

Five functions the AI can call:
- `log_water(amount_ml)` — logs water intake
- `log_sleep(hours, quality?)` — logs sleep
- `create_habit(name, frequency)` — creates a new habit
- `complete_habit(habit_name)` — marks a habit done today
- `get_health_summary()` — fetches recent data so the AI can answer "how am I doing" questions

### Security

The OpenAI API key must **never** appear in client code.

Use a **Supabase Edge Function** to create an ephemeral Realtime session token and return it to the app:

```
Client → Supabase Edge Function (authenticated) → OpenAI /v1/realtime/sessions → returns client_secret → Client uses token
```

### System Prompt

Build the system prompt dynamically from the user's profile and recent health data:

```ts
function buildSystemPrompt(profile: Profile, summary: HealthSummary): string {
  return `
You are Aurora, a warm and encouraging personal health companion.
The user's name is ${profile.name}. Their goals: ${profile.goals.join(', ')}.
Today: ${summary.todayWaterMl}ml water of ${profile.water_goal_ml}ml goal.
Sleep last night: ${summary.lastSleepHours}h (goal: ${profile.sleep_goal_hrs}h).
Habits done: ${summary.habitsCompleted}/${summary.habitsTotal}.
Memory: ${profile.memory_notes || 'None yet.'}.
Use your tools when the user mentions data — don't ask, just act and confirm.
Be concise. Max 2 sentences per response. Never be clinical.
  `.trim();
}
```

---

## Groq Rules (Insight Generation)

Use Groq (`llama-3.3-70b-versatile`) for generating the daily insight card text on the dashboard.

- Generate once per day, cache result in Supabase `insights_cache` table
- Check cache before calling Groq; skip generation if a valid insight exists for today
- Keep prompt concise — request one sentence, max 20 words
- Max tokens: 60

The Groq API key can be set as an `EXPO_PUBLIC_` env variable for the client in the hackathon build. For production, proxy through a Supabase Edge Function.

---

## Feature Implementation Rules

When the user asks to build a feature:

1. Read this file first.
2. Identify exactly which files need to change.
3. Keep changes focused — do not rewrite unrelated code.
4. Follow existing patterns.
5. Ensure the feature works end-to-end.
6. Fix TypeScript errors before finishing.
7. Apply optimistic updates for any data mutations.

---

## Animation Rules

Use **React Native Reanimated v3** for:
- Progress ring fill animation (hero visual — make it beautiful)
- Onboarding screen transitions
- Mic button pulse while listening
- Progress bar width animation
- Card entrance animations (fade + slight translateY)

Use simple `Animated` API only for basic fade/scale where Reanimated would be overkill.

Never add animation for its own sake. Every animation should communicate state (filling, loading, active, complete).

---

## Voice UI Rules

The companion screen mic button is the entry point to the voice agent.

UI states to handle:
- **Idle:** Mic icon inside gradient circle, subtle glow ring
- **Listening:** Animated waveform bars (purple-pink gradient), pulse ring around mic button
- **Processing:** Gradient spinner or "thinking" animation
- **Speaking:** Audio waveform, transcript text streams in
- **Error:** Clear error message, retry option

The transcript view should show the conversation history (user + Aurora turns) in a scrollable list, styled like a calm chat interface — not a technical debug log.

The mic button uses the `gradients.ctaButton` gradient circle. This is the same visual language as the auth CTA.

---

## Screen-by-Screen UI Reference

### Auth / Login Screen
- Background: `gradients.authBg` (very soft lavender, top to bottom)
- Large fitness illustration — top 55% of screen
- White card rising from bottom — `borderRadius: 24, padding: 24`
- "Please Login" heading — Poppins Bold 24px, `colors.textPrimary`
- Input fields: icon + text, no background, `borderBottomWidth: 1, borderColor: colors.borderHairline`
- "Forget Details?" / "Create account" — `colors.textSecondary`, 13px
- CTA: Circular 56px button, `gradients.ctaButton`, white arrow icon, `borderRadius: radius.full`

### Dashboard / My Activities Screen
- Background: `colors.bgPrimary` (#FFFFFF)
- Top bar: grid icon (left, `colors.textSecondary`) + user avatar circle (right)
- "My Activities" — Poppins Bold 22px, `colors.textPrimary`
- Search bar: pill (`borderRadius: radius.pill`), `bgInput` fill, search icon + placeholder
- Time tabs: "Daily | Weekly | Monthly | Yearly"
  - Selected: `gradients.primary` pill, white text
  - Unselected: no background, `colors.textSecondary` text
- 2×2 card grid (each card: `radius.lg`, white, shadow):
  - **Steps**: SVG ring with `gradients.stepsRing` stroke, value center, edit icon
  - **Sleep**: `gradients.sleepCard` gradient card, white text, moon icon
  - **Heart**: gradient bar chart (using `gradients.heartBars`), bpm label
  - **Calories**: SVG ring with `gradients.caloriesRing` stroke, kcal value center
- List rows: "Daily Meals" / "Other Information" — icon right-aligned, `colors.borderHairline` separator

### Session / Workout Screen
- Background: `colors.bgPrimary`
- "Current Session" — `colors.textSecondary` eyebrow, 13px
- Session time "40 min" — Poppins Bold 40px, `colors.textPrimary`
- "Medium Intensity" — `colors.textSecondary`
- Schedule date strip: horizontal scroll, each day as chip (`radius.sm` 8px)
  - Unselected: white chip, shadow, `colors.textSecondary`
  - Selected: `colors.gradientStart` background, white text, notification bell below
- "Your Progress" label + progress bar container
  - Track: `colors.bgInput` (#F5F5F8), `height: 6`, `borderRadius: radius.full`
  - Fill: `gradients.progressBar` LinearGradient, animates left → right
  - Thumb: white circle, `width: 20, height: 20, borderRadius: radius.full`, gradient border ring
- "40 min / 150 min" — `colors.textSecondary` 13px
- Running illustration — bottom right, decorative

---

## TypeScript Rules

Use TypeScript strictly.

Avoid `any`. All types live in `types/index.ts`.

Key interfaces to define early:

```ts
interface Profile { ... }
interface HydrationLog { ... }
interface SleepLog { ... }
interface Habit { ... }
interface HabitCompletion { ... }
interface HealthSummary { ... }
interface AgentTool { ... }
```

---

## Environment Variables

```bash
# .env.local — never commit this file

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_GROQ_API_KEY=
EXPO_PUBLIC_GOOGLE_CLIENT_ID=

# Server-side only (Supabase Edge Function secrets)
OPENAI_API_KEY=
```

Variables prefixed with `EXPO_PUBLIC_` are available in the client bundle.

The `OPENAI_API_KEY` must only exist in Supabase Secrets — never in the app bundle.

---

## Authentication Flow

```
App Launch
  │
  ├─ No session → (auth) Landing → Login / Signup
  │
  └─ Session exists
       │
       ├─ profile.onboarding_done = false → (onboarding) flow
       │
       └─ profile.onboarding_done = true  → (tabs) Dashboard
```

A trigger in Supabase auto-creates a `profiles` row on new user signup.

---

## Error Handling Rules

All API failures must show a user-friendly message. Never show a raw error string or crash the screen.

Every screen needs an empty state — design it to feel encouraging, not broken.

Example:
- Hydration: "Start your day with a glass of water 💧"
- Habits: "No habits yet. Let's build your first one."
- Sleep: "Log tonight's sleep to start tracking your patterns."

---

## MVP Scope Reminder

**In scope (must work):**
Intro screens · Auth (email + Google) · Onboarding · Dashboard · Hydration module (with bottle animation) · Sleep module · Habit module · Voice AI companion with agent actions · Daily insight card · Profile & Settings

**Stretch (build if time allows):**
Nutrition module · Streak counters · Health memory notes · Weekly summary screen

**Out of scope (this build):**
Device integrations · Apple Sign-In · Monthly reports · Production notifications · Backend beyond Supabase + Edge Functions

---

## Code Simplicity Rules

Avoid overengineering.

Refactor only when repetition or complexity clearly demands it.

A working, readable 50-line component is always better than an elegant, abstract 200-line system.

---

## Communication Style

Be concise.

Explain what changed and how to test it.

After each feature, tell the user:
1. What files were created or changed
2. How to test the feature
3. What to do next

---

## Final Reminder

Before every implementation:

- Read this file
- Follow it strictly
- Build clean, simple, polished code
- Replicate UI exactly when designs are provided
- Keep the voice AI working — it is the centrepiece of this app
- When in doubt, build less and make it beautiful

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.